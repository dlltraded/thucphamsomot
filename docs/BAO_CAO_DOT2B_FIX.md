# BÁO CÁO ĐỢT 2b-FIX (G1–G6) | Trạng thái: chờ rà soát
1. **G1 (API khách hàng):** Tạo 3 route dùng service-role + `can()`: `GET /api/admin/customers/list` (lọc search/nhóm/chưa phân công/thiếu liên hệ, không lộ password_hash, sale chỉ xem khách mình), `POST /api/admin/customers/assign-rep` (admin/truong_phong, ≤200 id, trả số dòng thực đổi), `POST /api/admin/customers/change-code` (regex `^TPS1-[A-Z0-9]{2,16}$`, unique). UI CustomersPage + CustomerDetailPage đã chuyển sang gọi API.
2. **G2 (Script --apply):** Kiểm tra bắt buộc có cột `kiotviet_code` (dừng nếu chưa chạy migration); in tóm tắt + yêu cầu gõ `YES`; `MATCH_PHONE` chỉ điền trường trống, không ghi đè name/company; ghi log lỗi từng dòng ra `tmp/import_errors_*.json`; kiểm tra lỗi khi tạo `customer_addresses`.
3. **G3 (Báo cáo dry-run):** Xóa toàn bộ số cứng, tính động 100%; thêm bảng 3 SĐT lỗi (`TML`, `KYOEI`, `SANBAY`); thêm chi tiết dòng `MATCH_PHONE` (`CAFE` vs `TPS1-100002` "Nguyen Tien Tien") cảnh báo lệch tên; hỗ trợ cờ `--skip-match`.
4. **G4 (Nối bộ sinh mã):** Viết migration `20260920d_customer_code_generation.sql` nối `generate_partner_code` vào `register_customer_account` (bỏ sequence số). RPC `admin_create_customer` nằm trên Dashboard nên không sửa trong repo.
5. **G5 (SQL Test):** Tạo file `tps1-miniapp/supabase/tests/generate_partner_code.sql` với 7 test case (Tân Vạn, Hoa Sen, ĐĐT, FGL U2, N.GỖ, cắt 12 ký tự, trùng suffix) để Claude/anh chạy trên Dashboard (chưa chạy trên DB).
6. **G6 (Đối chiếu hạn mức công nợ):** Cả 3 nơi (`PosCreatePage:485`, `reports/debt`, `bulk-confirm`) dùng cùng nguyên tắc: `credit_limit > 0` mới kiểm tra (bằng 0 hoặc null nghĩa là KHÔNG áp hạn mức); nợ tính từ các đơn khác `canceled` chưa thanh toán đủ (`debt_amount` hoặc `grand_total - paid_amount`).
7. **File đã tạo/sửa:** `app/api/admin/customers/{list,assign-rep,change-code}/route.ts`, `20260920d_customer_code_generation.sql`, `generate_partner_code.sql`, `import-kiotviet-customers.mjs`, `CustomersPage.tsx`, `CustomerDetailPage.tsx`, `PHASE1_TIEN_DO.md`, `tmp/import-khach-20260920.md`, `tmp/doi_chieu_ma_khach_20260920.csv`.
8. **Kiểm tra:** `npx tsc --noEmit` Root & `sale-webapp` đều exit code 0; dry-run script chạy thành công với và không có `--skip-match`.
