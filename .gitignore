.next/
node_modules/
TPS1/
.env
.env.local
*.log
dist/
out/
*.pdf
!.env.example

# File chứa credential thật dùng để test tay tại local, không được commit
check_order.cjs
check_rpc.cjs

# Export dữ liệu hàng hóa/giá thật từ KiotViet — dữ liệu kinh doanh, không đưa lên repo public
data/imports/

# Dọn dẹp dung lượng repo (2026-09-10) — file tạm/backup/asset không phục vụ
# website, từng bị commit nhầm, gây phình dung lượng deploy trên Vercel.
tmp/
*.zip
*_backup.*
_preview_*.jpg
FB_ADS_TPS1_TEST_ASSETS/
