const { createClient } = require('@supabase/supabase-js'); const supabase = createClient('https://yntgxollwjemyidizhnn.supabase.co', 'sb_publishable_BhQX_aNaD5wzocEp7MXD_Q_DA4kOAZn'); async function clear() { const { data } = await supabase.from('quotes').select('id'); if (data) { for (const row of data) { await supabase.from('quotes').delete().eq('id', row.id); } console.log('Deleted', data.length, 'rows'); } else { console.log('No data'); } } clear();
